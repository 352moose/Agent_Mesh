---
type: note
scope: TEMPLATES/SUBAGENT_template
maintained_by: OVERSEER
created: 2026/08/09
review_status: protected
---

# TEMPLATES — Sub-agent template

Blank form — copied to create a new one, never edited in place as though it were live.

---

## Dive Card

> Guide: not a walk stop — deliver the fenced block verbatim only when the user takes the dive option at the shelf fork.

```
── Tour Stop — AGENTS/TEMPLATES/SUBAGENT_template/

The blank form for a helper — the short-lived kind a seat spawns to do
one bounded job.

It is much smaller than a seat's form, and the differences are the
interesting part. There is no memory section, because a helper keeps
none. There is no closing procedure, because it doesn't close, it
ends. What it does have is a tight statement of scope and a list of
what it may not touch, since the whole safety of the arrangement is
that a helper can only do the one thing it was handed.
```

---
type: living_doc
scope: CLIDE — {PROJECT_NAME}
maintained_by: CLIDE
updated:            # YYYY/MM/DD
review_status: protected
---

# CLIde Active — {PROJECT_NAME}

> Project-tied memory card. Loaded after project selection, overwritten at spindown. Tied to the project, not the agent — a new project starts from a fresh copy of this template.

## Page Rules

| Rule |
|---|
| FACTS ≤25 lines — one line per fact, cross-cutting gotchas + pointers only |
| Best practices for the next CLIde on THIS project — not a session log, not a knowledge dump |
| Detail lives where it is used: code comments at fix sites, CODEBASE_ORIENTATION, ACTION_REPORT — this card holds a pointer at most |
| Overwrite at spindown per these rules; hold the write until the live-launch gate passes |

## FACTS

```
(empty — the first CLIde session on this project seeds it)
```
